import { Request, Response } from 'express';

const { validationResult } = require('express-validator');

export const getSuppliesPage = async (req: Request, res: Response) => {};

export const addSupply = async (req: Request, res: Response) => {};

export const removeSupply = async (req: Request, res: Response) => {};
